import { CfaCurrencyPipe } from './cfa-currency.pipe';

describe('CfaCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new CfaCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
